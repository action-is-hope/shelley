import { Meta, Canvas, Story, ArgsTable, Source } from "@storybook/addon-docs";
import dedent from "ts-dedent";
import { ActionButton, ButtonGroup, Icon } from "../../indexLib";
import { StoryCenteredInline } from "../StoryLayout";

export const BasicActionButton = (args) => (
  <StoryCenteredInline>
    <ActionButton {...args}>Edit</ActionButton>
  </StoryCenteredInline>
);

<Meta
  title="Inputs/Action Button"
  argTypes={{
    tone: {
      options: [1, 2, 3, 4],
      control: { type: "inline-radio" },
      description: "Tone of the button",
    },
    variant: {
      options: ["primary", "secondary", "quiet", "fab"],
      control: { type: "inline-radio" },
    },
    vol: {
      options: [1, 2, 3, 4, 5, 6],
      control: { type: "inline-radio" },
    },
  }}
  component={ActionButton}
/>

# Action Button

ActionButtons allow users to perform an action. They're used for similar, task-based options within a workflow, and are ideal for interfaces where buttons aren't meant to draw a lot of attention.

<Source
  language="tsx"
  code={dedent`
    import { ActionButton } from "@actionishope/shelley";
  `}
/>

<Story
  name="a) Checkbox"
  args={{
    children: "Subscribe",
  }}
  parameters={{
    docs: {
      source: {
        code: dedent`<Checkbox>Subscribe</Checkbox>`,
      },
    },
  }}
>
  {BasicActionButton.bind({})}
</Story>

## Button variants

The Button comes with four variants: primary (default), secondary, quiet and fab.

<Preview>
  <Story name="b) Button">
    <StoryCenteredInline>
      <ActionButton isQuiet>isQuiet</ActionButton>
      <ActionButton>Secondary</ActionButton>
      <ActionButton>Quiet</ActionButton>
      <ActionButton
        icon={
          <Icon>
            <path d="M14 7h-5v-5h-2v5h-5v2h5v5h2v-5h5v-2z"></path>
          </Icon>
        }
        variant={"fab"}
      >
        Fab
      </ActionButton>
    </StoryCenteredInline>
  </Story>
</Preview>

Break

<Story name="c) Icon + Text">
  <StoryCenteredInline>
    <ActionButton
      iconPos="start"
      icon={
        <Icon>
          <path d="M14 7h-5v-5h-2v5h-5v2h5v5h2v-5h5v-2z"></path>
        </Icon>
      }
      variant="primary"
      iconPos="end"
    >
      Add
    </ActionButton>
  </StoryCenteredInline>
</Story>

## P

## Button Props

<ArgsTable of={ActionButton} />
