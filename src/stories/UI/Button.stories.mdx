import {
  Meta,
  Canvas,
  Story,
  Preview,
  ArgsTable,
  Source,
} from "@storybook/addon-docs";
import dedent from "ts-dedent";
import { Button, ButtonGroup, Icon } from "../../indexLib";
import { StoryCenteredInline } from "../StoryLayout";

export const BasicButton = (args) => (
  <StoryCenteredInline>
    <Button {...args}>Text label</Button>
  </StoryCenteredInline>
);

<Meta
  title="Inputs/Button"
  argTypes={{
    tone: {
      options: [1, 2, 3, 4],
      control: { type: "inline-radio" },
      description: "Tone of the button",
    },
    variant: {
      options: ["primary", "secondary", "quiet", "fab"],
      control: { type: "inline-radio" },
    },
    vol: {
      options: [1, 2, 3, 4, 5, 6],
      control: { type: "inline-radio" },
    },
  }}
  component={Button}
/>

# Button

<Source
  language="tsx"
  code={dedent`
    import { Button } from "@actionishope/shelley";
  `}
/>

Buttons allow users to take actions, and make choices, with a single tap.

Buttons communicate actions that users can take. They are typically placed throughout your UI, in places like:

- Modal windows
- Forms
- Cards
- Toolbars

<Preview>
  <Story name="a) Button">{BasicButton.bind({})}</Story>
</Preview>

<ArgsTable of={Button} />

## Button variants

The Button comes with four variants: primary (default), secondary, quiet and fab.

<Preview>
  <Story name="b) Button variants">
    <StoryCenteredInline>
      <Button variant="primary">Primary</Button>
      <Button variant="secondary">Secondary</Button>
      <Button variant="quiet">Quiet</Button>
      <Button
        icon={
          <Icon>
            <path d="M14 7h-5v-5h-2v5h-5v2h5v5h2v-5h5v-2z"></path>
          </Icon>
        }
        variant={"fab"}
      >
        Fab
      </Button>
    </StoryCenteredInline>
  </Story>
</Preview>

## Primary buttons

Primary buttons are high-emphasis, distinguished by their use of obvious actionable styling like a fill. They contain actions that are primary to your app.

<Preview>
  <Story name="c) Primary button">
    <StoryCenteredInline>
      <Button>Primary</Button>
      <Button disabled>Disabled</Button>
      <Button as="a" href="#text-buttons">
        Link
      </Button>
    </StoryCenteredInline>
  </Story>
</Preview>

## Secondary buttons

Secondary buttons are medium-emphasis buttons. They contain actions that are important but aren't the primary action in an app.

Secondary buttons are also a lower emphasis alternative to primary buttons, or a higher emphasis alternative to quiet buttons.

<Preview>
  <Story name="d) Secondary button">
    <StoryCenteredInline>
      <Button variant="secondary">Secondary</Button>
      <Button variant="secondary" disabled>
        Disabled
      </Button>
      <Button variant="secondary" as="a" href="#text-buttons">
        Link
      </Button>
    </StoryCenteredInline>
  </Story>
</Preview>

## Quiet buttons

Quiet buttons are typically used for less-pronounced actions, including those located: in dialogs, in cards. In cards, text buttons help maintain an emphasis on card content.

<Preview>
  <Story name="e) Quiet button">
    <StoryCenteredInline>
      <Button variant="quiet">Quiet</Button>
      <Button variant="quiet" disabled>
        Disabled
      </Button>
      <Button variant="quiet" as="a" href="#text-buttons">
        Link
      </Button>
    </StoryCenteredInline>
  </Story>
</Preview>

## Floating Action Button (FAB)

A floating action button (FAB) performs the primary, or most common, action on a screen.

A floating action button appears in front of all screen content, typically as a circular shape with an icon in its center.

Only use a FAB if it is the most suitable way to present a screen's primary action. Only one component is recommended per screen to represent the most common action.

<Preview>
  <Story name="f) Floating action button (FAB)">
    <StoryCenteredInline>
      <Button
        icon={
          <Icon>
            <path d="M14 7h-5v-5h-2v5h-5v2h5v5h2v-5h5v-2z"></path>
          </Icon>
        }
        variant={"fab"}
        vol={1}
      >
        Label
      </Button>
      <Button
        icon={
          <Icon>
            <path d="M14 7h-5v-5h-2v5h-5v2h5v5h2v-5h5v-2z"></path>
          </Icon>
        }
        variant={"fab"}
        vol={2}
      >
        Label
      </Button>
      <Button
        icon={
          <Icon>
            <path d="M14 7h-5v-5h-2v5h-5v2h5v5h2v-5h5v-2z"></path>
          </Icon>
        }
        variant={"fab"}
        vol={3}
      >
        Label
      </Button>
    </StoryCenteredInline>
  </Story>
</Preview>

## Tone

The Button has the option to set the tone of the button according to variables set up as part of the theme. You may not need to use these at all if you don't really use other colours in your UI but they are customisable should you need them with 10 slots available which follow a number index so as not to impose a fixed nomenclature.

<Preview>
  <Story
    name="g) Tone"
    args={{
      variant: "primary",
    }}
  >
    <StoryCenteredInline>
      <Button variant="primary" tone={1}>
        Tone 1
      </Button>
      <Button variant="primary" tone={2}>
        Tone 2
      </Button>
      <Button variant="primary" tone={3}>
        Tone 3
      </Button>
      <Button variant="primary" tone={4}>
        Tone 4
      </Button>
    </StoryCenteredInline>
  </Story>
</Preview>

## Volume

The Button has the option to set the `volume` to define how 'loud` it should be. This is purely a number index with 1 begin the smallest and 6 being the largest. Set them up in your themes as you wish.

<Preview>
  <Story
    name="h) Volume"
    args={{
      variant: "primary",
    }}
  >
    <StoryCenteredInline>
      <Button variant="primary" vol={1}>
        Vol 1
      </Button>
      <Button variant="primary" vol={2}>
        Vol 2
      </Button>
      <Button variant="primary" vol={3}>
        Vol 3
      </Button>
      <Button variant="primary" vol={4}>
        Vol 4
      </Button>
      <Button variant="primary" vol={5}>
        Vol 5
      </Button>
      <Button variant="primary" vol={6}>
        Vol 6
      </Button>
    </StoryCenteredInline>
  </Story>
</Preview>

## Upload button

<Preview>
  <Story
    name="i) Upload button"
    args={{
      variant: "primary",
    }}
  >
    <StoryCenteredInline>
      <label htmlFor="contained-button-file">
        <input
          hidden
          accept="image/*"
          id="contained-button-file"
          multiple
          type="file"
        />
        <Button variant="primary" as="span">
          Upload
        </Button>
      </label>
    </StoryCenteredInline>
  </Story>
</Preview>

## Buttons with icons and label

Sometimes you might want to have icons for certain buttons to enhance the UX of the application as we recognize logos more easily than plain text. For example, if you have a add button you can label it with an add icon.

<Preview>
  <Story
    name="j) Icon and label buttons"
    args={{
      variant: "primary",
    }}
  >
    <StoryCenteredInline>
      <Button
        icon={
          <Icon>
            <path d="M14 7h-5v-5h-2v5h-5v2h5v5h2v-5h5v-2z"></path>
          </Icon>
        }
        variant="primary"
        iconPos="end"
      >
        Add
      </Button>
      <Button
        icon={
          <Icon>
            <path d="M14 7h-5v-5h-2v5h-5v2h5v5h2v-5h5v-2z"></path>
          </Icon>
        }
        variant="secondary"
        iconPos="start"
      >
        Add
      </Button>
      <Button
        icon={
          <Icon>
            <path d="M14 7h-5v-5h-2v5h-5v2h5v5h2v-5h5v-2z"></path>
          </Icon>
        }
        variant="quiet"
        iconPos="end"
      >
        Add
      </Button>
    </StoryCenteredInline>
  </Story>
</Preview>

## Icon button

Icon buttons are commonly found in app bars and toolbars.

Icons are also appropriate for toggle buttons that allow a single choice to be selected or deselected, such as adding or removing a star to an item.

<Preview>
  <Story
    name="k) Icon button"
    args={{
      variant: "primary",
    }}
  >
    <StoryCenteredInline>
      <Button
        icon={
          <Icon alt="Add">
            <path d="M14 7h-5v-5h-2v5h-5v2h5v5h2v-5h5v-2z"></path>
          </Icon>
        }
      />
    </StoryCenteredInline>
  </Story>
</Preview>

## Button Props

<ArgsTable of={Button} />
